export interface charactersType {
  id: number;
  name: string;
  imageUrl: string;
}

export interface characterType {
  id: string;
}

export interface characterDetailType {
  id: number;
  films: string[];
  name: string;
  imageUrl: string;
  sourceUrl: string;
}
